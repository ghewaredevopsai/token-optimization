# Token Optimization - note for whoever edits this repo

This repository is **public and cloned by participants**. `trainer/` ships with it deliberately:
the labs are measured rather than scored, so there are no spoilers to protect.

⚠️ **One thing must never be added here:** the key mapping the slides' participants A-D to real
sandbox identities, the cohort number, the real spend figures, and the names of internal
infrastructure. That lives in `../token-optimization-trainer/key.md`, with the cohort snapshot JSON
that is the only surviving copy of the data.

Run `trainer/delivery-map.md` section 8's leak scan before every push.
